import React from "react";
import logo from "../assets/logo_trustlayer.svg"

const NavBar = (props) => {

    return (
      <div id="navbar">
        <img src={logo}></img>
      </div>
    );
}

export default NavBar